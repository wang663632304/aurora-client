<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.vivido.aurora.client.auroraclient"
    android:versionCode="1"
    android:versionName="0.0.1-SNAPSHOT"
    android:configChanges="orientation" >

    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity android:name="it.vivido.aurora.client.base.AuroraActivity" />
        <activity android:name="it.vivido.aurora.client.base.AuroraSensorActivity" />

        <activity android:name=".DebugModeActivity" />
        <activity android:name=".DebugActivity" />
        <activity android:name=".TestGraphLayout" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".MonitorActivity" />
        <activity android:name=".FrontActivity" />
        <activity android:name=".PrefsActivity" />
        <activity android:name=".MsgActivity" />
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>